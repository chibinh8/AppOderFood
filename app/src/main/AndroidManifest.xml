<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.orderfood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".SignUp">

        </activity>

        <activity android:name=".Homepage">

        </activity>
        <activity android:name=".ThemBanAn"
            android:theme="@style/PopupActivity">

        </activity>

        <activity android:name=".ThemThucdon"></activity>

        <activity android:name=".ThemLoaiMonan"
            android:label="@string/ThemTD"
            android:theme="@style/PopupActivity">
        </activity>

        <activity android:name=".SoluongMonActivity"
            android:theme="@style/PopupActivity">

        </activity>
        <activity android:name=".Thanhtoan"
            ></activity>

    </application>

</manifest>